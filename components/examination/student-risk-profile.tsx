import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { getT } from "@/lib/i18n/get-translator";
import { Bdi } from "@/components/shared/bdi";
export interface StudentSubjectRisk { subject:string; tests:number; average:number; failed:number; trend:number|null; }
export interface StudentTestRow { date:string; title:string; subject:string; marks:string; status:"Pass"|"Fail"|"Absent"; }
export interface StudentRiskProfileProps { name:string; rollNo:string; className:string; riskScore:number; average:number; failed:number; subjects:StudentSubjectRisk[]; tests:StudentTestRow[]; weakTopics:string[]; }
export async function StudentRiskProfile({name,rollNo,className,riskScore,average,failed,subjects,tests,weakTopics}:StudentRiskProfileProps){
  const t=await getT();
  const risk=riskScore>=70?t("coordinator.studentRiskProfile.riskHigh"):riskScore>=35?t("coordinator.studentRiskProfile.riskWatch"):t("coordinator.studentRiskProfile.riskLow");
  return <main className="flex flex-col gap-5 p-4 sm:p-6"><div><h1 className="text-xl font-semibold text-neutral-900"><Bdi>{name}</Bdi></h1><p className="mt-1 text-sm text-neutral-500"><Bdi>{className}</Bdi> · {t("coordinator.studentRiskProfile.rollWord")} #<Bdi>{rollNo}</Bdi></p></div><div className="grid gap-3 sm:grid-cols-3"><Card><CardContent className="py-4"><p className="text-3xl font-semibold">{riskScore}</p><p className="text-xs text-neutral-500">{t("coordinator.studentRiskProfile.riskScore")} · <span className="font-medium text-neutral-700">{risk}</span></p></CardContent></Card><Card><CardContent className="py-4"><p className="text-3xl font-semibold">{average}%</p><p className="text-xs text-neutral-500">{t("coordinator.studentRiskProfile.overallAverage")}</p></CardContent></Card><Card><CardContent className="py-4"><p className="text-3xl font-semibold">{failed}</p><p className="text-xs text-neutral-500">{t("coordinator.studentRiskProfile.failedTests")}</p></CardContent></Card></div><Card><CardHeader><CardTitle>{t("coordinator.studentRiskProfile.subjectRisk")}</CardTitle></CardHeader><CardContent>{subjects.length?<div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead><tr className="border-b border-neutral-200 text-xs text-neutral-500"><th className="pb-2">{t("coordinator.studentRiskProfile.subject")}</th><th className="pb-2">{t("coordinator.studentRiskProfile.tests")}</th><th className="pb-2">{t("coordinator.studentRiskProfile.average")}</th><th className="pb-2">{t("coordinator.studentRiskProfile.fails")}</th><th className="pb-2">{t("coordinator.studentRiskProfile.trend")}</th></tr></thead><tbody>{subjects.map(s=><tr key={s.subject} className="border-b border-neutral-100 last:border-0"><td className="py-2 font-medium"><Bdi>{s.subject}</Bdi></td><td className="py-2">{s.tests}</td><td className="py-2">{s.average}%</td><td className="py-2">{s.failed}</td><td className="py-2">{s.trend==null?'—':`${s.trend>0?'+':''}${s.trend}%`}</td></tr>)}</tbody></table></div>:<p className="text-sm text-neutral-500">{t("coordinator.studentRiskProfile.noSubjectResults")}</p>}</CardContent></Card><div className="grid gap-4 lg:grid-cols-2"><Card><CardHeader><CardTitle>{t("coordinator.studentRiskProfile.weakTopics")}</CardTitle></CardHeader><CardContent>{weakTopics.length?<div className="flex flex-wrap gap-2">{weakTopics.map(topic=><Badge key={topic} variant="warning"><Bdi>{topic}</Bdi></Badge>)}</div>:<p className="text-sm text-neutral-500">{t("coordinator.studentRiskProfile.noWeakTopics")}</p>}</CardContent></Card><Card><CardHeader><CardTitle>{t("coordinator.studentRiskProfile.recentTests")}</CardTitle></CardHeader><CardContent>{tests.length?<div className="flex flex-col gap-2">{tests.map((test,i)=><div key={`${test.title}-${i}`} className="flex items-center justify-between rounded-xl bg-neutral-50 p-3"><div><p className="text-sm font-medium text-neutral-900"><Bdi>{test.title}</Bdi></p><p className="text-xs text-neutral-500"><Bdi>{test.subject}</Bdi> · <Bdi>{test.date}</Bdi></p></div><span className="text-xs font-medium">{test.status==='Absent'?t("coordinator.studentRiskProfile.absent"):<Bdi>{`${test.marks} · ${test.status}`}</Bdi>}</span></div>)}</div>:<p className="text-sm text-neutral-500">{t("coordinator.studentRiskProfile.noTestsRecorded")}</p>}</CardContent></Card></div></main>;
}

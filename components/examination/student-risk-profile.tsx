import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
export interface StudentSubjectRisk { subject:string; tests:number; average:number; failed:number; trend:number|null; }
export interface StudentTestRow { date:string; title:string; subject:string; marks:string; status:"Pass"|"Fail"|"Absent"; }
export interface StudentRiskProfileProps { name:string; rollNo:string; className:string; riskScore:number; average:number; failed:number; subjects:StudentSubjectRisk[]; tests:StudentTestRow[]; weakTopics:string[]; }
export function StudentRiskProfile({name,rollNo,className,riskScore,average,failed,subjects,tests,weakTopics}:StudentRiskProfileProps){const risk=riskScore>=70?"High":riskScore>=35?"Watch":"Low";return <main className="flex flex-col gap-5 p-4 sm:p-6"><div><h1 className="text-xl font-semibold text-neutral-900">{name}</h1><p className="mt-1 text-sm text-neutral-500">{className} · Roll #{rollNo}</p></div><div className="grid gap-3 sm:grid-cols-3"><Card><CardContent className="py-4"><p className="text-3xl font-semibold">{riskScore}</p><p className="text-xs text-neutral-500">Risk score · <span className="font-medium text-neutral-700">{risk}</span></p></CardContent></Card><Card><CardContent className="py-4"><p className="text-3xl font-semibold">{average}%</p><p className="text-xs text-neutral-500">Overall average</p></CardContent></Card><Card><CardContent className="py-4"><p className="text-3xl font-semibold">{failed}</p><p className="text-xs text-neutral-500">Failed tests</p></CardContent></Card></div><Card><CardHeader><CardTitle>Subject-by-subject risk</CardTitle></CardHeader><CardContent>{subjects.length?<div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead><tr className="border-b border-neutral-200 text-xs text-neutral-500"><th className="pb-2">Subject</th><th className="pb-2">Tests</th><th className="pb-2">Average</th><th className="pb-2">Fails</th><th className="pb-2">Trend</th></tr></thead><tbody>{subjects.map(s=><tr key={s.subject} className="border-b border-neutral-100 last:border-0"><td className="py-2 font-medium">{s.subject}</td><td className="py-2">{s.tests}</td><td className="py-2">{s.average}%</td><td className="py-2">{s.failed}</td><td className="py-2">{s.trend==null?'—':`${s.trend>0?'+':''}${s.trend}%`}</td></tr>)}</tbody></table></div>:<p className="text-sm text-neutral-500">No subject-level results yet.</p>}</CardContent></Card><div className="grid gap-4 lg:grid-cols-2"><Card><CardHeader><CardTitle>Weak topics</CardTitle></CardHeader><CardContent>{weakTopics.length?<div className="flex flex-wrap gap-2">{weakTopics.map(t=><Badge key={t} variant="warning">{t}</Badge>)}</div>:<p className="text-sm text-neutral-500">No weak topics identified from the available data.</p>}</CardContent></Card><Card><CardHeader><CardTitle>Recent tests</CardTitle></CardHeader><CardContent>{tests.length?<div className="flex flex-col gap-2">{tests.map((t,i)=><div key={`${t.title}-${i}`} className="flex items-center justify-between rounded-xl bg-neutral-50 p-3"><div><p className="text-sm font-medium text-neutral-900">{t.title}</p><p className="text-xs text-neutral-500">{t.subject} · {t.date}</p></div><span className="text-xs font-medium">{t.status==='Absent'?'Absent':`${t.marks} · ${t.status}`}</span></div>)}</div>:<p className="text-sm text-neutral-500">No tests recorded.</p>}</CardContent></Card></div></main>}
